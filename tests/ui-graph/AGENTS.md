# AGENTS

## Scope
- Keep tests in this folder focused on `@pdp-helper/ui-graph` view-model shaping and layout invariants.

## Workflow
- Prefer small package-level tests that protect framework-agnostic graph helpers.
- Use `@pdp-helper/contracts-graph` fixtures rather than app- or service-specific test data.
