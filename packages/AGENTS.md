# Packages Folder Guide

- Read the repo root `AGENTS.md` and `docs/agentic-workflow.md` first.
- `packages/contracts-*` owns public contracts.
- `packages/runtime-*` owns shared execution scaffolding.
- `packages/ui-*` owns reusable UI primitives.
